---
name: document-current-state
description: "Business-facing constraints page for PM/sales; optional vault MCP if configured."
agents:
  - cursor
---

/caveman full

**Repo cockpit:** `.cursor/STRUCTURE.md` (vault / Obsidian MCP policy, stakeholder doc tone) + `.cursor/CONTEXT.md`.

**Bind:** user names product/concept; Obsidian MCP only if STRUCTURE / user says vault live.

**Sections:** scope paragraph; in-scope bullets (verifiable); limitations + workaround + ticket ref; dependencies; risks; stakeholder questions — **complete** list (no fixed count cap; “≤7 questions” is a soft default only when brevity requested).

**Rule:** capability language — no code dump. Label assumptions vs verified.

Vault missing → say so; ask user paste canonical note or skip.
