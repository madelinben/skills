---
name: test-ui-ux
description: "Manual responsive / theme / overflow checklist after test-plan output or feature list."
agents:
  - cursor
---

/caveman full

**Repo cockpit:** `.cursor/STRUCTURE.md` (viewport list, theme procedure) + `.cursor/CONTEXT.md`.

**Bind:** STRUCTURE for viewport widths + theme switch procedure.

Viewports: mobile / tablet / desktop (+ wide if STRUCTURE lists tables).

Check: overflow; touch targets; theme switch; skeleton vs jump; error copy; deep link refresh.

**Output:** markdown table — area | viewport | pass/fail | notes — **all** cells filled for scope; use “N/A” with reason where not applicable.
